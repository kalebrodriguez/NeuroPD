# AGENTS.md

## Cursor Cloud specific instructions

This repository currently contains only a placeholder `README.md` (NeuroPD research title). There is **no application source code**, dependency manifests (`package.json`, `requirements.txt`, `pyproject.toml`, etc.), Docker Compose, tests, or lint configuration yet.

Until application code is added:

- There are **no services to start**, no lint/test/build/run commands, and nothing to install beyond the empty update script (`true`).
- Do not invent a stack (Python/ML, notebooks, web app, etc.) — wait for committed project files and follow whatever README/CONTRIBUTING they introduce.
- After code lands, update this section with non-obvious startup caveats (required services, env vars, hello-world smoke steps) and replace the update script with the appropriate dependency refresh command(s).
