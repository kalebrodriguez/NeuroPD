"""Configurable, auditable EEG preprocessing (scaffold).

Concrete steps and parameters (filter cutoffs, notch frequency, epoch length,
reference, artifact thresholds) must not be hard-coded before inspecting the
data (spec Section 10.1); they are implemented in Milestone 2 from configuration.
"""
